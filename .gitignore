# ── Build artifacts ──────────────────────────────────────────────
/target

# ── Packaged builds (AppImage, portable-zip, AppDir) ─────────────
/dist

# ── Runtime / local ──────────────────────────────────────────────
/logs
*.log

# Ядро zapret (стратегии Flowseal + движок) кладётся сюда вручную при
# разработке либо тянется кнопкой «Скачать ядро»; в репозитории не хранится.
/core

# ── Editor / OS / agent ──────────────────────────────────────────
/.claude/
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini
